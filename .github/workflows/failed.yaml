name: Test Failed Jobs

on:
  pull_request:

jobs:
  retest-test:
    name: Run Retest Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - name: Run go test ./internal/actors/retest
        run: go test ./internal/actors/retest
